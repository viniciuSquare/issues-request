const issuesRouter = require('express').Router()

// GET ALL ISSUES
issuesRouter.get('/', );

// GET SINGLE ISSUE
issuesRouter.get('/:id', );

// CREATING NEW ISSUE
issuesRouter.post('/', );

// UPDATE AN ISSUE
issuesRouter.put('/:id', );